name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.9 is transitional; drop it (and the matching pyproject/CI bits)
        # once users have moved off it.
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        run: pipx install poetry

      - name: Install dependencies
        run: poetry install --no-interaction

      - name: Lint (flake8)
        run: poetry run flake8 pynetdicom2 tests

      - name: Lint (pylint)
        run: poetry run pylint pynetdicom2 --fail-under=9

      - name: Type check (mypy)
        run: poetry run mypy

      - name: Run tests with coverage
        run: |
          poetry run python -m coverage run -m unittest discover -s tests
          poetry run python -m coverage report
